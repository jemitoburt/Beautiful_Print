from setuptools import setup, find_packages

setup(
    name='Beautiful_Print',
    version='0.1.0',
    packages=find_packages(),
    install_requires=[
        'colorama',
        'datetime',
    ]
)